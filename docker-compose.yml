services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/data.db:/app/data.db

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
